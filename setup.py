from setuptools import setup, find_packages

setup(
    name='SortingSearchLibrary',
    version='1.0.0',
    packages=find_packages(),
    description='Custom Sorting and Search Algorithms Library',
    author='Adrija',
    install_requires=[],
)
